# OS
.DS_Store
Thumbs.db
desktop.ini

# IDEs
.idea/
.vscode/
*.iml
*.swp
*~

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
build/
dist/
.venv/
venv/
env/
.env
.env.local
.env.*.local
backend/.env
backend/.python-version
backend/.venv/

# Node
node_modules/
.pnpm-store/
.next/
out/
.turbo/
*.tsbuildinfo
dashboard/.env
dashboard/.env.local
dashboard/.next/
dashboard/package-lock.json

# Android / Gradle
.gradle/
build/
local.properties
*.apk
*.aab
*.keystore
*.jks
captures/
.cxx/
android-agent/app/build/
android-agent/.gradle/

# Docker
infra/.data/

# Backups / logs
*.log
logs/

# Secrets (defense in depth)
*.pem
*.key
secrets/

# Editor / tooling temp files
*.bak
*.tmp

# Stray files with parentheses in their names (macOS / shell-mangled
# filenames from copy-paste of things like "EnrollScreen(onEnrolled)"
# into bash on Windows). Empty and useless — ignore instead of tracking.
*(*)
*)*

# Experimental one-off deploy driver (Playwright) — kept out of the
# tree. Use deploy.ps1 + the Render dashboard instead.
render-deploy.js
render-step*.png
render-fail-*.png